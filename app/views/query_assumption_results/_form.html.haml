= semantic_form_for question, remote: true do |f|
  = f.inputs do
    = f.input :result, as: :radio, collection: {"Yes": true, "No": false, "Not answered": nil}
  = f.actions do
    = f.action :submit, :as => :input