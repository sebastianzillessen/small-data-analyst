= semantic_form_for @user do |f|
  = f.inputs do
    = f.input :email
    = f.input :approved
    = f.input :role,  :as => :select, :collection => User::ROLES

  = f.actions do
    = f.action :submit, :as => :input
