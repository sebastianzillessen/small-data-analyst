= semantic_form_for @research_question do |f|
  = f.inputs do
    = f.input :name
    = f.input :description
    = f.input :private
    = f.input :models, :as => :select, :collection => Model.order(:name).all.select { |a| can? :read, a }

  = f.actions do
    = f.action :submit, :as => :input
