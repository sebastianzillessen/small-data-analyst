%h1
  User waiting for approval on #{root_url}

%p
  A new user with the email #{@user.email} has registered on #{root_url}.
  Please go to #{link_to(admin_user_url(@user), admin_user_url(@user))} to activate him.

