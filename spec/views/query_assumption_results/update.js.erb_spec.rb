require 'rails_helper'

RSpec.describe "query_assumption_results/update", type: :view do
  before(:each) do
    @query_assumption_result = assign(:query_assumption_result, create(:query_assumption_result))
  end

  it "renders attributes in <p>" do
    render
    skip "write specs"
  end
end
